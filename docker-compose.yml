version: '3.0'
services:
  web:
    image: pingwinno/recorder
    ports:
      - "8080:8080"
    volumes:
      - settings:/etc/streamarchive
      - /Users/mromanc/streams:/root/streams
volumes:
  settings: